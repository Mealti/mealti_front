import { View, Text } from "react-native";
import React from "react";
import AuthContent from "components/auth/AuthContent";

const SignUpScreen = () => {
  const handlePress = () => {};
  return <AuthContent handlePress={handlePress} />;
};

export default SignUpScreen;
